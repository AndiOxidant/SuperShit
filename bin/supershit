#!/usr/bin/env node

const path = require('path')
const supershit = require('../')

const cmdPaths = [
  path.join(process.cwd(), 'bin'),
  path.join(__dirname)
]

supershit.commandLine(cmdPaths, process.argv)
